# Uncomment the next line to define a global platform for your project
plugin 'cocoapods-art', :sources => [
'cocoa-pro-fphi'
]

source 'https://cdn.cocoapods.org/'

target 'demosdk' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'IQKeyboardManagerSwift', '~> 7.0.1'

  pod 'FPHISDKVideoRecordingComponent', '~> 2.9.0'
  pod 'FPHISDKSelphIDMBSDRComponent', '~> 2.9.0'
  pod 'FPHISDKCaptureComponent', '~> 2.9.0'
  pod 'FPHISDKDisclaimerComponent', '~> 2.9.0'
  pod 'FPHISDKMainComponent', '~> 2.9.0'
  pod 'FPHISDKSelphiComponent', '~> 2.9.0'
  pod 'FPHISDKStatusComponent', '~> 2.9.0'
  pod 'FPHISDKTokenizeComponent', '~> 2.9.0'
  pod 'FPHISDKTrackingComponent', '~> 2.9.0'
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    target.build_configurations.each do |config|
      if target.name == 'zipzap'
        config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '9.0'
      end
      config.build_settings['EXPANDED_CODE_SIGN_IDENTITY'] = ""
      config.build_settings['CODE_SIGNING_REQUIRED'] = "NO"
      config.build_settings['CODE_SIGNING_ALLOWED'] = "NO"
    end
  end

  # CocoaPods reorders build phases on pod install; keep compose copy after [CP] phases.
  installer.aggregate_targets
    .map(&:user_project)
    .uniq
    .each do |project|
      project.targets.each do |target|
        next unless target.name == 'demosdk'

        copy_phase = target.shell_script_build_phases.find { |phase| phase.name == 'Copy compose resources' }
        next unless copy_phase

        target.build_phases.delete(copy_phase)
        target.build_phases << copy_phase
      end
      project.save
    end
end
